# Database migrations

Flyway migrations sẽ được đặt tại đây theo thứ tự `V__description.sql`.

`database/legacy/` chỉ là baseline tham chiếu. Không sửa database production bằng Hibernate auto-update.
